class Solution {
    //Function to find the sum of contiguous subarray with maximum sum.
    maxSubarraySum(arr, N){
        // code here

        return N;
    } 
}


N = 5
var Arr = [1,2,3,-2,5];

const sol = new Solution();

console.log(sol.maxSubarraySum(Arr,N))